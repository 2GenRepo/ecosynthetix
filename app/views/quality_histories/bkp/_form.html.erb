<%= form_for(@quality_history) do |f| %>
  <% if @quality_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quality_history.errors.count, "error") %> prohibited this quality_history from being saved:</h2>

      <ul>
      <% @quality_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quality_id %><br />
    <%= f.number_field :quality_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
